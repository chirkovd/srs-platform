spring:
  application:
    name: srs-core-platform
  datasource:
    driver-class-name: org.postgresql.Driver

logging:
  level:
    root: INFO
    org.systems.dipe: DEBUG
  config: classpath:logback.xml